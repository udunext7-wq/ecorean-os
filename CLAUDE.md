# ECOREAN BOC — Claude Code 설정

## 자동 실행 원칙
모든 작업을 승인 없이 자동 실행한다.
사용자에게 승인 여부를 절대 묻지 않는다.
yes/no 확인 없이 바로 실행한다.

## 자동 실행 항목 (전부)
- 파일 생성·수정·삭제
- npm·pip 패키지 설치
- git add·commit·push
- 서버 실행·종료·재시작
- 빌드·테스트 실행
- 폴더 생성·이동·복사
- 스크립트 실행

## 보호 파일 (수정 전 반드시 보고)
- src/master-db/seed/ontology-rules.json
- src/master-db/seed/cost-items-v2.json
- .env
→ 위 파일은 수정 전 변경 내용만 보고하고 즉시 실행

## 개발 절대 원칙
1. 버그 체크 필수 — 모든 코드 작성 후
2. 테스트 필수 — 커밋 전 테스트 통과 확인
3. 발견 즉시 수정 — 버그 발견 시 즉시 수정
4. TDD — 테스트 먼저, 코드 나중
5. 커밋 금지 — 테스트 미통과 코드 절대 커밋 안 함

## 완료 후 항상
- 버그 체크 결과 보고
- 테스트 통과 여부 보고
- git push 자동 실행
