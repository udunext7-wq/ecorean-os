# MiniCAD — 소스 위치·작업 규칙 (SSoT)

> **실서비스 소스 = `sites/net/public/minicad/`** (ecorean.net/minicad/ 로 배포되는 바로 그 파일)
> 2026-08-19 대표 지시로 루트 `minicad/` 분리본·Desktop `MiniCAD-v5.9-Galaxy` 사본을 폐기하고 여기로 일원화.
> 구 사본은 `archived/minicad-legacy-split-20260819/` (untracked, 배포 제외)에 보존.

## 파일 구조 (로드 순서 = 의존 순서)
```
sites/net/public/minicad/
├── index.html          진입점 — 모든 <script>/<link> 에 캐시버스터 ?v=TAG (변경 시 전부 일괄 bump)
├── css/style.css       테마(lime/architect) + 2026-08-19 태블릿 규칙(pointer-coarse, 퀵바, 통합 메뉴)
├── fonts/
└── js/
    ├── state.js        STATE 싱글턴
    ├── data.js         SPACE_TYPES / FLOOR_MATERIALS / WALL_MATERIALS / 라이브러리 데이터
    ├── library.js      라이브러리 심볼 도형
    ├── engine.js       Konva 스테이지·그룹·renderAll·VEF·스냅
    ├── sketch.js       2026-09-04 점·선·면 스케치 엔진 + 자유 매스 (순수 계산, node require 가능 — tests/minicad-3d.cjs 가 직접 검증)
    ├── tools.js        initTools() — 도구/이벤트(mousedown·touchstart…)·contextmenu·키보드
    ├── ui.js           initUI() — 패널·명령창·JSON·견적·refreshUI(포커스 보존 래퍼)
    ├── touch.js        initTouch() — 태블릿 터치·S펜 레이어 + 통합 컨텍스트 메뉴
    ├── estimate.js     자동 견적서(단가표)
    └── tests.js        자체 테스트 (?test=1) — 커밋 전 통과 필수
├── 3d/                 2026-09-01 3D 뷰 (별도 탭, Coohom 식 1단계) — [🧊 3D] 버튼 / `3d` 명령
│   ├── index.html      뷰어 페이지 (library.js + build3d.js + importmap → view3d.js)
│   ├── build3d.js      문서(JSON) → 3D 기본체 목록. THREE 무관 순수 계산 — 노드 테스트 tests/minicad-3d.cjs
│   └── view3d.js       three.js 장면·조감/걷기·조명·클릭 이름표·PNG/GLB/JSON 내보내기
└── vendor/three/       three.js r170 (module 빌드 + OrbitControls + GLTFExporter, MIT) — 외부 CDN 없음
```
3D 동기화: MiniCAD `open3DView()`(ui.js) 가 문서를 localStorage `minicad.3d.doc` 로 넘기고 새 탭을 연다. 이후 `saveHistory`/undo/redo 가 `push3D()` 를 불러 BroadcastChannel `minicad-3d` 로 300ms 묶음 전송 → 3D 탭이 즉시 다시 조립한다. 3D 탭이 먼저 열려 있으면 `hello` 를 보내와 전송이 켜진다.
3D 는 평면 데이터만으로 세운다 — 벽(두께·높이·마감색)/문·창(폭·높이·창턱, 인방·창턱 자동)/바닥·천장(공간 폴리곤)/가구·기구(종류별 높이 프로파일, build3d.js `FURN_H` 등)/조명(천장 높이에 부착, 발광 + 포인트라이트 최대 24)/기둥/계단(직선 도식). 벽 정렬(interior/exterior)은 2D `_wallAlignOffsetPx` 와 같은 규칙(`wallAlignOffset`, 일반벽=우측 법선 ±t/2 · 내력벽=내력벽 무게중심 방향)으로 몸체를 밀고, 꺾여 만나는 모서리는 이웃 벽 바깥 면까지 끝을 늘려 메운다(`cornerExtension`).
GLB 내보내기는 2단계(Blender/AI 렌더)용 — 객체 이름이 `kind:이름` 으로 들어간다.

### 층(Floor) 시트 — 2026-09-03
- 대표 요구: 층을 한 시퀀스에 겹쳐 그리면 z 도 없고 버벅임 → **층마다 별도 시트, 저장은 한 문서**.
- 구조: **활성 층만 STATE 배열에 산다**(도구·렌더·견적·인쇄 전부 활성 층 대상 — 다른 층 렌더 비용 0). 잠든 층은 `STATE.floors[].data` 에 JSON 스냅샷(undo 스냅샷과 같은 방식), 히스토리도 층별 보관. 핵심 함수: `switchFloor`/`addFloor({copy})`/`deleteFloor`/`renameFloor`/`_floorsExport`(ui.js).
- UI: 상단 브랜드 옆 `#floor-bar` 탭(클릭=전환, 더블클릭=이름, [+]=빈 층, Shift+[+]=구조 복제), 명령 `fl`·`fl 2`·`fl add`·`fl copy`·`fl del 2`·`fl name 이름`.
- 저장 스키마: 최상위 배열 = 활성 층(기존 스키마 그대로) + `floors:[{id,name,level,active}|{...,data}]` + `activeFloorId` → 서버 저장(`buildJSON`)·자동저장(`buildAutosavePayload`)·3D 전송 모두 한 문서. 옛 저장본(floors 없음)은 1층짜리로 열린다. 견적 프로파일에서는 floors 제거.
- 복제 시 `_remapFloorIds` 로 id 접미사 재부여(전 층이 한 문서/3D 에 섞이므로 유일해야 함).
- 3D: `buildScene` 이 `splitFloors` 로 층을 나눠 층마다 `buildFloorScene` → `obj.z0`(층 바닥 표고 = 아래층 누적 층높이, 층높이=최대 천장고+슬래브 300mm) 로 적층. 다층이면 obj.id 에 `층id:` 접두. 뷰어는 층 필터 버튼(전층/층별), 걷기 모드는 선택 층 눈높이로.
- 주의: 견적·인쇄·DXF·AI 번들은 **활성 층 기준** — 전 층 합산이 필요하면 층을 돌며 합쳐야 한다(미구현).
- 층 삭제: 활성 탭의 ✕(확인창) 또는 `fl del`(현재 층)/`fl del 2`. 마지막 한 층은 못 지운다.

### 클립보드 Ctrl+C/V/X — 2026-09-03
- 선택된 **모든 종류** 복사(`copySelection`): 공간은 소속 벽·문창·안의 객체 동반(`spaceContainedObjects`), 벽은 꼭짓점·그 벽의 문창 동반. 좌표 굳힘은 undo 스냅샷과 같은 JSON 왕복.
- 붙여넣기(`pasteClipboard`): `_remapFloorIds` 로 id 재부여(v1Id/vertexIds/wallId/spaceId·회로 lightIds/lightGang/jumpIds 까지) → **마우스 커서 위치**로 이동(커서 없으면 +600mm) → reinstallVEF → 붙인 것들 선택 상태. 벽 없이 온 문창은 `findNearestWallId` 재부착. 잠금은 해제되어 붙는다.
- 클립보드는 층 바깥(전역 `_clipboard` + localStorage `minicad.clipboard`) — **다른 층·다른 탭**에 붙여넣기 가능. Ctrl+X = 복사+삭제(잠긴 객체는 deleteSelected 가 걸러줌).
- 키 가드: INPUT/TEXTAREA 포커스·텍스트 드래그 선택 중·인쇄 설정창 열림에는 브라우저 기본 동작 유지.
- 테스트: tests.js [CP] 11건 (커서 위치·다중·공간 동반·층 건너·원상복구).

### 미니폼(MiniForm) = 3D 모듈 제품명 (2026-09-04 대표 명명)
UI 표기·대화 모두 "미니폼". 코드 식별자(3d/ 경로·MC3D·명령 `3d`)는 유지.

### 스케치업 인터페이스 구조 전면 카피 (2026-09-04 대표 지시)
- **메뉴 바**(#menubar, 8개): 파일(PNG Ctrl+S·GLB·JSON·다시 받기) / 편집(undo·redo·삭제·선택해제) / 보기(조명·야간·천장·이름표·그림자·축 — ✓ 체크 표시) / 카메라(조감·걷기·아이소·위·정면·측면·전체) / 그리기(선 L·사각형 R) / 도구(전체) / 창(트레이·섹션) / 도움말(단축키표). 배선=`menuCmd`(view3d.js), 호버 전환·바깥 클릭 닫힘.
- **Default Tray**(#tray, 우측 5섹션 아코디언): 개체 정보(#props — 미선택 시 안내문 상주) / 재질(#paintpal — 고르면 페인트 도구 자동) / 구성요소(#addpal — 고르면 배치 도구 자동) / 스타일·표시(토글 6종) / 강사(#instructor — setTool 마다 도구 안내). `openTraySec`/`setTray`, 창 메뉴로 접기.
- **Large Tool Set 순서**(좌측): 선택 | 선·사각형 | 이동·밀기끌기·회전·배율 | 줄자 | 페인트·지우개 | 배치 | 팬·줌 (구분선 .tsep).
- **축 표시**: X빨강·평면Y초록·높이파랑(`buildAxes`, 보기 메뉴/스타일 패널 토글, build 마다 범위 재계산).
- 단축키 추가: **Ctrl+S=PNG 저장**, **?=단축키표 모달**(#keysmodal, Esc 닫힘 — Esc 는 모달→메뉴→동작취소→선택해제 순).
- 천장/이름표 토글은 `toggleCeil`/`toggleLabels` 로 일원화(툴바·메뉴·스타일 패널 공용), 상태 동기화는 `refreshStylePanel`.
- 스모크 검사: menus=8·traySecs=5·axes·instr.

### 점·선·면 구조 (2026-09-04 대표 지시 — 스케치업 기하 모델)
- **매핑**: 점=Vertex(STATE.vertices) · 선=벽/Edge(walls, v1Id/v2Id 공유) · 면=공간/Face(spaces, vertexIds) · **그룹=공간 단위**(면이 생기면 점·선·면+내부 배치가 spaceId 로 한 그룹 — 복사/삭제/이동 동반).
- ~~**미니폼 R(사각형) = 면 생성**: op `addspace`~~ → **2026-09-04 프로토콜 6 에서 `sketchrect`(스케치 면)로 대체** — 아래 "점·선·면 → 매스" 참조. `addspace`/`addwall`/`addcircle` op 는 호환용으로만 남음(뷰어는 더 이상 보내지 않음, 테스트가 금지 확인).
- **미니폼 L(선) = 면 위에서 분할**: 뷰어 `segHitsSpace` 가 선분이 면을 가로지르는지 판정(양끝 내부/경계 교차 2회) → op `splitspace` → 2D `addLine`(분할·못 가로지르면 참조선, 잠긴 공간 보호). ~~빈 곳이면 `addwall`~~ → 빈 곳이면 `sketchline`(스케치 선). 잠든 층 분할은 거부+안내.
- **점·선·면 스냅(추론)**: `snap3` — 끝점(초록 ≤180mm) > 중간점(청록 ≤160) > 선 위(빨강 ≤120) > 10mm 격자. 스냅 마커 구체 + VCB 라벨에 스냅 이름. 스냅이 직교 추론보다 우선(스케치업과 동일). 데이터는 build 때 층별 `ST.snapData`(verts/walls/spaces/stats).
- **Entity Info 그룹 요약**: 면 선택 시 "그룹: 면 1 · 선(벽) N · 배치 N" (snapData.stats).
- 스모크: snapF=2·snapEnd=endpoint·snapSplit=공간id·snapWall=null. 테스트 [EDf] 5건(면 생성/분할/각 면 벽 소유/잠든 층).
- 2026-09-04 원점 기준점: 2D drawGrid 에 X빨강/Y초록 축선+파란 원점 링(#axis-*, 그리드 off 유지·인쇄 제외) + **양쪽 원점(0,0) 스냅** — 2D snapToEndpoint 에 원점 후보(끝점과 동급), 미니폼 snap3 kind 'origin'(파랑 마커 '원점(0,0)'). 테스트 [AX] 6건·스모크 snapOrg.
- 2026-09-04 사각형 UX(대표 피드백): 호버 스냅 마커(클릭 전), 첫 점 고정 구슬(startMk), 크기=반투명 면 고스트, **치수 입력 `가로,세로`**(vcbPair, 끌던 방향 부호), **생성 딜레이 제거** — 3D 발 편집은 `push3D(true)` 즉시 회신 + 낙관적 고스트(pendingG, 다음 build 때 실물 교체). E2E cdp-face 가 msPlan(평면 반영)/msRound(실물 교체) 실측.

### 2026-09-07 대표 지적 — R 은 공간 그대로, 매스는 별도 도구(N)
- 프로토콜 6 이 `STATE.sketchMode=true` 로 **R(사각공간)·C(원형공간)·L(선) 의 뜻을 조용히 바꿔** 놓았다.
  도구 이름은 '사각공간' 인데 공간이 안 만들어졌다. 대표 지적: "기존 공간과 다른 개념으로
  단축키를 따로 두고 새로 만드는 방식으로 가야 한다."
- **되돌림**: `sketchMode` 기본값 `false`. R/C/L 은 종전대로 곧바로 공간·선을 만든다.
- **새 도구 `mass`(단축키 N, 명령 `ms`/`매스`)**: 바닥을 끌어 그리면 면 → `sk-z` 로 Z 를 묻고
  → 매스. 도구가 'mass' 이면 `sketchMode` 와 무관하게 언제나 이 길로 간다(`endRect`).
- 옛 프로토콜 6 방식이 필요하면 `sk on` 으로 되살린다(스위치는 남겨 뒀다).
- 미니폼(3D)의 R 은 스케치업 규약대로 **사각형(스케치 면)** 그대로 — 두 창의 도구 체계가 다르다.
- 테스트: `?test=1` [R1~R5] 5건(R=공간·N=면+Z·Z→매스·꼭짓점 높이·공간 전환·sk on 복원),
  SK38 은 '기본 꺼짐' 으로 뜻이 바뀜.

### 매스에 z 값 — 꼭짓점마다 높이 (2026-09-07 대표 지시 "나는 z 값을 원한다")
- **살아 있는 높이**: `h_mm`·꼭짓점 z 가 숫자 또는 참조 `{r:'ch'|'fh'|'fl', o:offset}`.
  천장고를 고치면 매달린 매스가 따라 움직인다. 사람이 숫자를 넣어도 참조는 지키고 오프셋만 고침(`zSet`).
- **자유 다면체**: `mass.solidVerts[{x,y,z}]` + `solidFaces[{vs,mat,roleFix}]`.
  **각기둥인 동안에는 옛 형식(`pts`+`h_mm`) 그대로 저장**하고, 꼭짓점 높이가 달라지는 순간
  `massToSolid` 로 승격, 다시 평평해지면 `massTryPrism` 이 되돌리며 새 필드를 지운다(호환).
  `massSolid(m,ctx)` 는 어느 쪽이든 완전한 다면체를 돌려준다.
- **면이 갈래를 안다**: 법선 → `floor|ceil|wall|slope` + `facing`·`tilt(°)`.
  **갈래는 저장하지 않고 늘 지금 형상에서 다시 잰다** — 저장했더니 면을 기울여도 '천장' 인 채로 굳었다.
- **접힘 정리(스케치업 Autofold)**: `splitFoldedRing` 이 이웃한 세 점의 평면마다 고리에서
  잇달린 조각을 찾아 **실제 접힌 선을 따라** 나눈다(부채꼴로 자르면 지붕 한가운데에 없는 세로
  삼각형이 생긴다 — 박공에서 실제로 겪음). **나눈 결과는 저장하지 않고 `massSolid` 가 읽을 때마다
  다시 나눈다** — 저장했더니 다음 편집이 잘못 잘린 조각 위에서 돌아 지붕이 엉켰다.
- **물량**: `massQuantities` → `{floor,ceil,wall,slope,ceilAll,maxTilt}`, `massVolume`(㎥).
  경사면은 눕힌 넓이가 아니라 **비탈을 따라간 실면적** — 4×3 방을 한쪽만 1200 올리면 천장 12→12.528㎡.
- **파이프라인**: `js/sketch.js`(계산 한 곳) → `build3d` 가 `t:'mesh'{verts,tris,faces}` → `view3d` 가
  BufferGeometry. **3d/index.html 이 sketch.js 를 함께 읽는다** — 두 벌로 베끼면 평면과 3D 가 조용히 어긋난다.
- 검증: `tests/minicad-3d.cjs` [Z1~Z8](손계산과 소수점 일치 — 빗천장 12.528㎡·박공 16.144㎡·부피 39.6㎥),
  헤드리스로 박공 지붕이 실제로 서는 것까지 확인. 캐시 `tablet109`/`3d25`.
- **손잡이 (2026-09-07 · Z축 3층)**: 엔진만 있고 만질 길이 없어 빗천장을 만들려면 JSON 을 손으로 써야 했다.
  · **미니폼 꼭짓점 그립** — 매스 하나를 고르면 윗면 꼭짓점마다 파란 공(`buildGrips`). 끌면 그 점만 Z
    (축 고정 불필요) · **Shift** = 화면에서 가까운 이웃까지 두 점 · **Ctrl** = 천장고에 매달기(금색 그립)
    · 숫자 입력(VCB) = 정확한 높이 · **그립 더블클릭** = 직전 높이 반복. 끄는 동안 `∠21.8° · 물매 4/10`.
    미리보기는 `sketch.js` 를 그대로 불러 만든다(`_vzPreview`) — 3D 에서 형상을 새로 짜면 접힘·갈래가
    두 벌이 되어 평면과 답이 갈라진다.
  · **프로토콜 7** — `setz{id,verts:[{i,z}]}` · `settop{id,z}` · `zref{id,verts:[i],r,o}`. **배열을 통째로
    주고받지 않는다** — 두 창이 동시에 고치면 서로를 덮어쓴다. 받는 쪽 `_apply3DZ` 는 `massVertZ`·
    `massSetTop` 을 부르기만 한다. 3D→2D 허용 필드에 `solidVerts` 는 **넣지 않는다**(전용 op 만 길).
  · **평면 표기** — 지붕 평면도 관례 그대로(`renderMassZ`): 꼭짓점마다 높이 숫자(매달린 값은 `CH-300`·금색),
    경사면 가운데 **내림 방향 화살표 + 물매 4/10**, **마루=굵은 실선 · 골=파선**. 화면에서 52px 미만이면
    숫자·화살표는 접는다(줌아웃 시 글자만 뭉친다). 각기둥에는 아무것도 안 그린다.
  · **속성 패널** — 꼭짓점 높이표(`_msZTable`). 칸에 `2700` 도 `CH-300`/`천장고+600`/`FH` 도 넣을 수 있다
    (`_msZParse`). [평평하게] [CH 에 매달기] 두 버튼. 경사 천장 ㎡·물매·부피를 함께 보여 준다.
  · **읽기용 헬퍼는 sketch.js 한 곳**: `massTopPts`(밑면 i ↔ 윗면 N+i 규약)·`massSlopes`(내리막 방향은
    법선의 (nx,ny))·`massRidges`(위를 보는 면 둘이 만나는 모서리 — 양쪽이 내려가면 마루, 올라가면 골)·
    `massCtx`·`massLean`(미리보기 사본)·`zEdit`(되받아 읽히는 짧은 표기)·`pitchOf`.
- **겪은 함정 4가지** (전부 테스트가 잡는다):
  1. `_gripAt` 이 `updateMatrixWorld` 없이 레이를 쏴 **막 세운 그립이 안 집혔다**(buildGrips 는 invalidate
     만 하므로 다음 rAF 전에는 행렬이 옛것). 하늘 배경 때와 같은 함정.
  2. 끌기 기준점을 **첫 pointermove** 로 잡아 그 한 걸음만큼 덜 올라갔다(800 을 끌었는데 600). 기준은 **누른 자리**.
  3. 입력 칸에 `zLabel`('천장고 -300 = 2100')을 넣었더니 **엔터를 치는 순간 못 알아들었다** → `zEdit`(`CH-300`).
  4. `evalDim` 은 못 읽으면 `null` 을 주는데 **`isFinite(null)` 은 true** 라, '가나다' 가 0 이 되어
     꼭짓점이 바닥에 처박혔다 → `typeof v==='number'` 로 막는다.
- **빗천장이 돈이 된다 (2026-09-07 · Z축 4층 · 설계서 5·6번)**
  · **공간이 제 천장을 안다** — `space.ceilMassId`. 공간을 자유 다면체로 바꾸지 **않는다**(바닥재·천장고·
    둘레를 아는 성질이 사라진다). 공간은 그대로 두고 천장 매스 하나를 가리킨다. `spCeilMass`/`spCeilQty`/
    `spCeilArea`/`spCeilTilt`(engine.js) — 지정이 없거나 매스가 평평하거나 죽은 참조면 **평면 면적으로 떨어진다**.
  · **견적** `computeQty(applies:'ceiling')` 가 `spArea` → **`spCeilArea`**. 4×3m 방 한 변을 1200 올리면
    천장 물량이 12 → **12.9244㎡**(=4×√(3²+1.2²)). 평면 면적으로 잡으면 도배·천장재가 물매만큼 모자란다.
  · **부속표** 걸레받이·몰딩 표의 천장㎡ 가 실면적(+`∠21.8°`), 새 표 **경사 천장·매스** (경사㎡·천장계㎡·물매·부피㎥,
    그 방 이름 표기). 합계도 실면적 합.
  · **지정은 사람이 한다** — 자동으로 안 붙인다(천장 면적이 곧 도배 금액이다). 2D 매스 패널 `[「거실」의 천장으로 지정]`
    (`massOverSpace` = 매스 무게중심이 들어앉은 방), 공간 패널에 빗천장 배지 + `[평천장으로 되돌리기]`,
    미니폼 매스 패널 `[▣ 아래 방의 천장으로]`. 어느 방인지는 **평면이 판단한다** — 3D 는 매스만 가리킨다.
  · **프로토콜 8** — `ceilmass{id,off?}`.
- **인쇄 조판 무한루프 방어 (같은 날 발견)**: `_p2Pack` 은 `rh`(행 높이)가 NaN 이면 `room` 이 NaN → `take` 가
  빈 배열 → `i` 가 안 늘어 **인쇄를 누른 순간 앱이 통째로 멈춘다**. 종이 값이 하나라도 이상하면 그렇게 된다.
  `_p2RowMm` 은 0·NaN 을 절대 안 내보내고, `_p2Pack` 은 `cap.h`/`cap.cols` 를 보정하며 한 바퀴에
  **최소 한 줄은 반드시 소비**한다. (`?test=1` Z31b 가 지킨다.)
- **박공 벽 — 벽 상단이 천장을 따라간다 (2026-09-07 · Z축 5층 · 설계서 7번)**
  · **숫자로 굳히지 않는다**: `wall.topFollowCeil=true` 는 '이 방의 천장 매스를 따라간다'는 뜻.
    지붕을 다시 기울이면 벽이 **저절로** 다시 잘린다(살아 있는 높이와 같은 사고).
    개별 지정은 `h1_mm`/`h2_mm`. 아무것도 없으면 종전대로 평평.
  · **두 끝만 읽으면 안 된다** — 마루 밑을 지나는 벽은 **가운데가 솟는다**.
    `massTopProfile(m,x1,y1,x2,y2,ctx)` 가 윗면 조각들의 경계와 만나는 자리를 모두 찾아
    `[{t,z}]` 옆모습을 돌려준다(곧게 이어지는 마디는 접는다). `profileAvg` = 사다리꼴 넓이 ÷ 길이.
  · **어디에 쓰이나**: 3D 는 마디마다 쐐기 mesh 를 얹고(모서리 연장부까지 기울기를 이어 간다),
    입면도는 사각형 대신 **옆모습 다각형**(박공 벽 = 오각형, 무늬도 그 안에만),
    벽면적 `spWall` 은 평균 높이로 — 견적이 정확해진다. 2D 벽 패널 `[▲ 천장에 맞추기]`/`[평평하게]`.
  · 천장을 지정·해제하면 그 방의 벽이 함께 붙고 떨어진다(`spWallsFollowCeil`).
- **감김 뒤집힘 버그 (같은 날, 이 작업이 드러냄) — 중요**
  `massSolid` 의 각기둥 갈래가 **입력 폴리곤의 감김 방향을 가정**하고 있었다. 그런데 매스 도구
  (`skAddRect`)가 만드는 감김은 **음수** — 즉 **도구로 그린 매스는 전부 안팎이 뒤집혀 있었다**.
  바닥이 `ceil` 로, 지붕 경사면이 `down` 으로 잡혀 `ceilAll`(천장 물량)이 0 이 되고,
  `massZAt` 이 바닥 높이를 돌려줬다. 손으로 쓴 시험 데이터가 전부 반대 감김이라 여태 안 드러났다.
  · 각기둥: `skPolyArea(poly)<0` 이면 **면의 감김만** 뒤집는다 — 꼭짓점 순서를 건드리면
    밑면 i ↔ 윗면 N+i 규약이 깨져 `massTryPrism` 이 영영 못 되돌린다.
  · 다면체: `_solidFlipIfInsideOut` 이 **부피 부호**로 안팎을 보고 모든 면을 돌린다(옛 파일 구제).
  · `massFromPoly` 는 아예 바른 감김으로 저장한다.
  · `?test=1` Z56 이 "매스 도구의 감김에서도 바닥이 바닥이다" 로 지킨다.
### 프리폼 (2026-09-07 대표 결정 — "견적은 미니캐드, 미니폼은 자유 렌더링")
- **왜**: 층·평면에 묶인 구조에서는 3D 자유 하나의 비용이 늘 3~4배(3D 조작+프로토콜+2D 표기+견적 의미).
  설계서 2단계(벽감·아치)가 막힌 것도 "평면도에 어떻게 적나"라는 질문 때문. 대표 결정 —
  **보낸 자료는 밑그림으로만 남고 독립 서비스**. 자동 병합은 업계(스케치업·아키캐드·레빗)도 안 한다.
- **구조 — 모드를 가른다** (연동을 버리는 게 아님):
  · 연동 뷰(기본): 종전 그대로 실시간 양방향 (프로토콜 8)
  · **프리폼 모드**(🧊 버튼 · 파일 메뉴): 들어가는 순간 평면이 **밑그림 층**으로 굳는다.
    그 위의 스케치·매스는 **자유 층** — 프리폼 자신의 문서(`ECOREAN.FreeForm.v1`,
    localStorage 자동 + 파일 저장/열기)·자신의 Ctrl+Z. 평면으로 아무것도 자동 전송 없음.
    평면이 바뀌면 받아만 두고(⚠ 표시) **[평면 다시 불러오기]** 로 밑그림 층만 재기준 — 자유 층 유지.
- **구현의 핵심 한 수**: 편집이 나가는 길이 `emitEdit` 하나 (직접 `chan.postMessage({type:'edit'` **0건** —
  테스트가 강제). 연동이면 채널로, 프리폼이면 `ffApply` 가 같은 op 을 자유 층에 그 자리에서 적용 —
  sketch.js 함수(bag 인자)를 그대로 불러 미니캐드 `_apply3DSketch` 와 같은 답. 도구·스냅·VCB·그립 한 벌이
  두 모드를 섬긴다. 자유 층은 `floorId:'freeform'` 가상 층(z0=0·층 캐시 밖) — `findGroup`/`floorOK`/
  스냅(`_snapDataOf` = 자유 층+1층 밑그림 병합)만 분기.
- **밑그림은 읽기 전용**: 이동·밀기끌기·페인트·지우개·그립 모두 자유 층만. 공간·벽 전환과 천장 지정은
  평면(견적)의 일이라 프리폼에서 거부. 배치물(가구)은 다음 단계.
- 검증: `cdp-ff.cjs` E2E 24건 — 진짜 포인터로 사각형→면→매스→그립 경사, **채널 전송 0건 확인**,
  자체 undo/redo, 밑그림 삭제 거부, 재기준(자유 층 유지), 재진입 복원, 스키마.
  연동 뷰 회귀 전부 통과(왕복 22·그립 21·두 창 E2E). 캐시 `3d25`→`3d26`.
- **② 3D 스케치 그래프 (같은 날 구현) — "아무 데나 그리면 면이 된다"**
  · **핵심 한 수**: 새 3D 기하를 발명하지 않았다. **평면마다 2D 그래프 하나**(`ffPlaneBag`) —
    벽면을 클릭하면 그 벽면이 자기 점·선·면 bag 을 갖고, 검증된 `skAddEdge/skAddRect/skDetectFaces`
    가 (u,v) 좌표로 그 안에서 돈다. 좌표계만 바꿔 끼운 것.
  · sketch.js: `planeFrom`(법선→정규직교 틀·결정적 — 벽이면 u 수평·v 위)·`planeUV`/`planePt`·
    `planeSame`(±법선 + 1mm)·`ffPlaneBag`·`planeExtrude`(면→법선으로 뽑아 자유 다면체,
    땅 밑이면 들어 올림). `massTopPts` 는 밑면↔윗면 짝 없는 자유 다면체에 그립 목록을 안 준다.
  · view3d: 그리기 도구(L/R)의 **첫 클릭이 면을 종이로 잡는다**(`_ffFacePick` — 레이캐스트 법선,
    땅바닥이면 종전 그대로). 이후 점은 레이∩평면(`_ff3UV`, 같은 평면 점 스냅 150mm + 10mm 격자),
    op `line3`/`rect3`(클릭 도구 등록·더블클릭=사슬 끝). **P 가 평면 면을 법선 방향으로 뽑는다**
    (`extrude3` — 화면 이동을 법선의 화면 투영에 내적, 벽 두께 밀기와 같은 수법. 음수=파내기는 ③).
  · build3d: `normalizeDoc` 가 `planes` 를 정규화, `buildPlaneSketch` 가 prim `face3`(uv 삼각화 후
    평면 틀로 — 오목 지원)/`edge3`(쿼터니언 원기둥)/`pt3` 로 세운다. kind 는 종전 스케치와 같아
    선택·삭제·속성이 한 벌로 돈다.
  · ffApply: sketch op 에 `patch.plane` 이 실리면 그 평면의 그래프로(`_ffBagFor`), `sketchdel`/`extrude`
    는 모든 그래프에서 찾고(`_ffFindFace`), 평면 면 extrude 는 `planeExtrude`. undo 는 자유 층
    스냅샷에 planes 가 포함돼 한 줄기.
  · 검증: node [P1~P7] 손계산(1.6㎡ 면 300 뽑기=0.48㎥·겉넓이 4.76㎡·땅 밑 들어 올림) ·
    `cdp-ff2.cjs` E2E 17건 — **진짜 포인터로 벽면에 사각형 → P 로 뽑아 벽에서 덩어리가 자란다**,
    채널 전송 0건, undo/redo·저장 왕복에 평면 그래프 포함. 캐시 `3d26`.
- **③ 파내기 (같은 날 구현) — 벽감·관통, CSG 없이**
  · **원칙 그대로**: 파냄은 `mass.cuts[]` 로 **기록만** 하고, 그릴 때마다 호스트 면에 구멍을 뚫고
    주머니(옆벽·바닥)를 다시 만든다(`massCutPrims`). 갈래·접힘을 저장 안 하는 것과 같은 사고.
  · **cut 은 매스 로컬 좌표**(`massLocalFrame`) — 매스를 옮기고 돌려도 벽감이 따라간다.
  · sketch.js: `massFaceAt`(호스트 면 + 다각형이 온전히 안에 있는지 — 걸치면 거부)·
    `massRayExit`(무게중심에서 -법선으로 나가는 거리 = 벽 두께)·`massAddCut`(두께 이상 밀면
    **관통** — d 를 두께로 자르고 through)·`massCutsVolume`(massVolume 이 뺀다).
  · build3d: 구멍 난 면은 fan 삼각화에서 빼고 `face3h`(바깥 고리+구멍 고리들, earcut 이 삼각화)로.
    관통이면 나란한 뒷면도 뚫는다. 주머니 옆벽·바닥은 살짝 어둡게(`_dim`) — 파임이 읽힌다.
    호스트 면이 (꼭짓점 편집으로) 기울어 사라지면 조용히 건너뛴다 — 렌더는 안 터진다.
  · view3d: **P 를 안으로 끌면 파내기**(extrude3 음수 — 고스트가 흙색으로), 숫자는 크기·방향은
    끌던 쪽. op `cut{id,d}` 은 프리폼 로컬 전용(채널로 절대 안 나감). 실패하면 면을 되살린다.
  · 검증: node [C1~C7] 손계산(벽감 250→28.4㎥·관통 d=3000·부피 27.32·로컬 저장·렌더 프림 구성) ·
    `cdp-cut.cjs` E2E 15건 — 진짜 포인터로 그리고 **안으로 밀어** 벽감·관통, 걸침 거부,
    undo 로 벽이 아물고 redo 로 다시 파임, 채널 0건. 캐시 `3d27`.
- **④ Follow Me (같은 날 구현) — 몰딩·걸레받이: 단면을 둘레 따라, 마이터로**
  · sketch.js: `sweepProfile(path,closed,base,profile)` — 모서리는 **마이터**(각의 이등분선에 단면을
    세우고 1/cos(θ/2) 늘림 — 목공이 45°로 켜서 잇는 그 방식, 스파이크는 4배에서 자름).
    바깥쪽 판정은 감김에 기대지 않고 점을 넣어 본다(`_pathOutSign` — 감김 버그를 이미 겪었으므로).
    되꺾임(180°)·오목 모서리(ㄱ자) 생존. 훑은 사각이 살짝 뒤틀려도 massSolid 의 접힘 정리가 받는다.
  · `moldingProfile('rect'|'crown'|'cove', w, h)` 단면 프리셋 · `massFromSweep` → 자유 다면체 매스
    (이동·삭제·undo 가 다른 매스와 동일, 색 목재 `#8B6F47`).
  · view3d: 프리폼에서 매스 패널에 **단면 w×h 입력 + [⌐ 걸레받이 (바닥 둘레)] [⌐ 천장 몰딩 (위 둘레)]**.
    op `followme{massId,at,profile}` 또는 `{pts,closed,z,profile}`(자유 경로 — 커튼박스 레일).
    빗천장(다면체) 위 둘레는 거부(윗면이 평평해야), 바닥은 됨. 프리폼 로컬 전용 — 채널 0건.
  · 검증: node [F1~F5] 손계산 — 14m 둘레 × 10×80 마이터 = **0.011232㎥**(띠 0.1404㎡×0.08),
    열린 2m = 0.0016㎥, 크라운 2340~2400, ㄱ자 생존, **감김 뒤집어도 같은 답**.
    `cdp-fm.cjs` E2E 12건 — 패널 버튼 실제 클릭으로 걸레받이·천장 몰딩, 자유 경로, 거부 3종, undo. 캐시 `3d28`.
- **⑤ 그룹·컴포넌트 (같은 날 구현) — 붙박이를 한 번 만들어 어디든 찍는다**
  · **그룹** = `mass.gid` 하나. 여럿 선택 → 패널 `[⛓ 그룹 묶기]`. **클릭=그룹 전체 · 더블클릭=하나만**
    (스케치업 손맛). 잡아 끌면 통째 이동, Ctrl+끌기=통째 복사(사본은 **새 gid** — 원본 그룹에 안 끼어든다,
    한 batch 안 사본들은 `FF._gidMap` 으로 같은 새 그룹). `[그룹 풀기]`.
  · **컴포넌트** = 그룹을 이름 붙여 문서에 저장(`FF.free.comps` — 무게중심 기준 상대좌표, 자동저장·
    파일 저장에 같이 실림). 구성요소 칸 맨 위 **🧊 내 컴포넌트** 목록 → 누르면 스탬프 모드:
    바닥을 클릭한 자리마다 독립 사본(새 gid)이 찍힌다 · Esc=끝. **살아 있는 연결(정의-인스턴스)은
    아니다** — 찍은 뒤는 서로 독립 (그건 달 단위 공사라 정직하게 뺐다).
  · op `group/ungroup/compsave/stamp` — 전부 프리폼 로컬(채널 0건), undo 한 줄기(comps 포함).
  · 검증: `cdp-grp.cjs` E2E 12건 — 패널 버튼으로 묶고, 진짜 포인터로 클릭(2개)/더블클릭(1개)/
    그룹째 끌기(같은 델타 실측), 프롬프트로 저장, 스탬프 2회(매스 6·그룹 3), Esc, 풀기, undo. 캐시 `3d29`.

- **프리폼 재질 (같은 날 구현) — B 페인트로 매스에 텍스처 (렌더 전용·견적 무관)**
  · `mass.mat` = 재질 코드. 팔레트(벽·바닥·천장 어느 분류든)에서 고르고 **B 로 매스 클릭** —
    다중 선택 위 클릭이면 한 번에. Alt+클릭=추출, 패널 `[지움]`, undo·재진입 유지.
  · 렌더: 프리즘=ExtrudeGeometry UV(m 단위)가 `floorMat` 텍스처와 그대로 맞고, **다면체는 삼각형
    주법선 축 박스 투영으로 UV 를 만들어** 입힌다. `face3h`(벽감 구멍 면·주머니 바닥)는 Shape UV 가
    mm 라 m 로 줄여 맞춤 — **원목 널결이 벽감 안까지 이어진다**. `_texCanvas` 밑색에 WALL_COLORS 추가.
  · op 는 기존 `set{mat}` (null=지움) — 프리폼 로컬, 채널 0건.
  · 검증: 조립 실측(본체·주머니·구멍 면 전부 mcode) + `cdp-mat.cjs` E2E 9건(팔레트 실제 클릭 →
    프리즘·다면체 페인트, UV 실존, 추출, 밑그림 차단, 지움·undo·재진입). 캐시 `3d30`.

- **프리폼 스테이징 (2026-09-08 구현) — 가구·조명을 자유 층에 (고객 제안 렌더의 마지막 조각)**
  · 구성요소 칸에서 고르면 G 도구 그대로 — 고스트 따라다니고 바닥 클릭에 놓인다. **자유 층에**
    (`FF.free.furniture/fixtures/lights/electric/hvac`) — 평면·견적 무관, 채널 0건.
  · `add` 가 라이브러리 검증 후 로컬 배치, 이동(끌기)·회전·복제(Ctrl)·삭제·잠금이 매스와 같은 길
    (`FF_PLACE` 분기). **스테이징 조명은 실제 포인트라이트로 빛난다**(ffRender 가 retunePointLights).
  · undo·자동저장·재진입 유지 (자유 층 스냅샷에 포함).
  · 검증: `cdp-stage.cjs` E2E 10건 — 팔레트 실제 클릭→고스트→바닥 클릭 배치(자리 실측), 조명
    포인트라이트 증가 실측, 끌어 이동, 회전·복제·삭제, undo/redo, 재진입. 캐시 `3d31`.

- **파랑 축 + 면 스냅 (2026-09-08 대표 지적 "빨강·초록은 되는데 파랑축으로는 작동이 안 된다")**
  · **원인**: 선 도구의 축 고정에 ↑(파랑)가 이동 전용이었다 — 2D 평면 그래프라 세로 선을 그릴 데가 없었다.
  · **↑ = 위로 그리기**: 땅에서 선을 긋다 ↑ 를 누르면 그 점을 지나는 **카메라를 바라보는 세로 종이**로
    올라탄다(세로 선 하나로는 평면이 안 정해지므로 — 스케치업도 보는 방향이 종이를 정한다).
    파랑↑·빨강→ 번갈아 허공에 ㅁ자를 닫으면 세로 면 → P 로 입체. line→line3 전환(`ff3Hop` 로직).
  · **면 위 선 축 고정**: →=가로(u·빨강) · ↑=세로(v·파랑) · ←/↓=해제. 고스트가 축 색으로.
    숫자 입력(`ff3Commit`)=잠긴 축(또는 지금 방향)으로 정확한 길이 — 사슬 이어짐.
  · **면 스냅 강화**(`_ff3SnapList`/`_ff3UV`): **매스 모서리(자유 층+밑그림)·평면 스케치**의
    끝점 > 중간점 > 선 위, 반경은 화면 14px(땅 그리기와 동일), 3D 스냅 마커(`_ff3Mark`).
    히스토리+평면 키로 캐시 — 마우스마다 solid 재계산 안 함.
  · 이동 ↑(파랑) 는 프리폼에서도 정상(E2E 실측 430mm) — 상태줄 문구만 정직하게
    (프리폼이면 "평면 반영" 대신 "(프리폼)").
  · 검증: `cdp-blue.cjs` E2E 10건 — ↑ 올라타기, 파랑·빨강 번갈아 ㅁ자(0.96㎡ 손계산)→입체 0.48㎥,
    모서리 10px 비켜 눌러도 (3000,4000,2000) 정확, 중간점+마커, 이동 파랑, 채널 0건. 캐시 `3d32`.

- **독립 프리폼 앱 (2026-09-08 대표 지시 — "허브 설계견적 미니캐드 밑에 프리폼이라는 3d모델링")**
  · **ecorean.net/freeform** → `/minicad/3d/?ff=1` 308 (vercel.json — 인증은 목적지의 minicad 게이트가 건다).
  · 허브 설계·견적, MiniCAD 바로 아래: **프리폼 — 3D 모델링, 스케치업식 자유 조형 (견적 무관)**.
  · `?ff=1` = `FF_STANDALONE`: 평면 없이 **빈 밑그림으로 곧장 프리폼 부팅**, 제목·브랜드 '🧊 프리폼',
    연동 뷰로는 못 나감(ffExit 차단). 저장은 브라우저 자동 + 프리폼 파일 — **새로고침해도 그대로**.
  · **미니캐드와 완전 단절 (대표 지시 "단독이기 때문에 연결하지 마라")**: BroadcastChannel 을 아예
    안 열고, storage 이벤트도 무시하고, 어떤 경로로 평면이 와도 받지도 알리지도 않는다.
    재기준·도면 다시 받기·평면 저장 메뉴는 숨김. E2E 가 채널 0개를 실측으로 강제.
  · 검증: `cdp-solo.cjs` E2E 7건(자동 부팅·그리기→뽑기→재질 로컬·ffExit 차단·새로고침 복원) +
    계약 5건(허브 항목·위치·라우트). Next 빌드 통과. 캐시 `3d33`.

### 빛의 모양 (2026-09-09 대표 지시 "동그란 등은 동그랗게, 긴 간접조명은 일직선처럼")
- 종전: 모든 등이 가운데 점 광원 하나 — 선형 등(T5·라인·간접 코브·형광·주방 플랫·
  리니어 펜던트·스팟바·트랙·마그네틱 트랙)도 둥근 웅덩이 빛이 났다.
- build3d: 선형 타입에 `meta.lightLen`(발광 길이) 도장 — 프림이 전부 로컬 X 로 길게
  놓이는 규약을 그대로 쓴다. view3d: `subsOf()` 가 길이/600 (2~5개) 광원을 로컬 X 를
  따라 줄지어 달아 **빛 자체가 일직선** — 등의 회전(angle)을 그룹 좌표로 자동 추종.
  동그란 등은 종전대로 점 하나 = 둥근 풀. 광원 예산(demand=Σ광원수)에 그대로 계산되고,
  빠듯하면 등마다 하나(선형은 가운데) → 격자 묶음 순으로 물러난다.
- 검증: 노드(라운드 lightLen 0·T5 2400·코브 1500·뷰어 계약), E2E `cdp-shape.cjs` —
  T5 광원 4개 펼침 1800mm, 90° 회전 시 줄이 Z 로, 코브 3개, 다운라이트 1개.
  스위트 1313/1315·회로 점등·라이트마다 광원·단독 프리폼 회귀 통과. v=3d45.

### 라이트마다 실제 광원 (2026-09-09 대표 지시 "전원을 키면 군데군데 들어온다")
- 종전: MAX_POINT_LIGHTS=24 상한을 stride 표본(i%stride)으로 맞춰, 등이 24개를 넘으면
  몇 개 걸러 하나만 실제 광원이 달렸다 — 발광 갓은 다 빛나도 바닥·벽의 빛 풀은 군데군데.
  (앞선 "첫선·끝선에만 표시" 지적의 실제 뿌리도 이 표본이었다.)
- **`_plBudget()`** — GPU 프래그먼트 유니폼(maxFragmentUniforms)에서 광원 예산을 계산
  (광원 1개≈vec4 4개, 재질 몫 200 제외, 24~200). 예산 안에서는 **켜진 등마다 제 광원**.
- 예산 초과(수백 등)면 2.5m 격자로 근접 등을 묶어 √n 배 세기의 대표 광원 — 표본과 달리
  어느 등도 격자 대각(≈3.6m) 안에 광원이 있어 빈 구역이 없다.
- autoPerf: 광원 48개↑ 픽셀비 1.5, 96개↑ 1.0 로 상쇄. `MC3DVIEW._plBudget` 진단 노출.
- 검증: E2E `cdp-pl.cjs` — 40등=광원 40(종전 표본이면 20), 300등=예산 이하 묶음·최대 이격
  1.9m 실측, 노드 계약(stride 금지·예산·격자). 스위트 1313/1315·단독 프리폼·회로 점등 회귀 통과. v=3d44.

### 회로 점등 — 미니폼이 등마다 스위치를 따른다 (2026-09-09 대표 지시)
"불이 들어온 표시가 스위치 연결 첫선과 끝선에만 표시된다 — 불이 들어오면 모든 등에
불이 들어온 것처럼, 실제 빛 표현처럼."

- 종전 미니폼은 회로를 통째로 무시했다: 전역 조명 토글 하나로 모든 램프가 켜지거나
  꺼졌고, 스위치를 조작해도 3D 는 그대로였다.
- **build3d `circuitLightState(D)`** — 2D `litLightIds()` 와 같은 규칙의 순수 재현(문서만 읽음):
  켜진 구(gangOn[lightGang]) 직결 조명이 시드, 점핑(jumpIds, 양방향) 연쇄 확장.
  gangOn 없는 옛 문서는 circuitOn. 어느 스위치에도 안 닿는 미배선 등 = 종전처럼 켜짐
  (회로 안 그린 도면이 통째로 어두워지지 않게). `meta.on` + 꺼진 등의 발광 프림에 `lit:false`.
- **view3d** — 재질 캐시 키에 lit 포함(켜짐/꺼짐 별도 재질), 꺼진 갓은 발광 0 + 색 0.55배(잿빛),
  Blender 이름 `_emitoff`. 전역 조명 토글은 마스터: ON 이어도 회로가 끈 등은 소등.
  포인트라이트(실제 빛 풀)는 켜진 등에만 단다 — 꺼진 등이 24개 슬롯을 먹지 않는다.
- 2D 에서 구 토글 → saveHistory → push3D → 미니폼 즉시 반영 (기존 경로 그대로).
- 검증: 노드 회로 스위트(구별·점핑·옛 문서·프림 도장·뷰어 계약 3), E2E `cdp-lit.cjs` —
  실창에서 구 OFF/ON 토글·점핑 연쇄·미배선·전역 마스터·포인트라이트 개수 11건.
  데스크톱 1313·터치 1315·단독 프리폼·파랑축 전부 통과. v=3d43.

### 천정 도구 — 프리 / 빠른작업 (2026-09-09 대표 지시)
"도구에 천정버튼을 만들고 천정버튼 속성에 프리와 빠른작업을 만들어줘. 프리는 내가 직접
만드는 것이고 빠른작업은 기존 바닥을 이용해서 만드는 거야."

- **천정 = 매스.** 이동·회전·되돌리기·미니폼·프리폼 기계를 전부 그대로 얻는다.
  `mass.ceil=true`, `h_mm=낮춤(단내림)`, `elev_mm=천장고−낮춤`, 색 `#D9CFBE`, 이름 `천정N`.
  천장고는 그 자리 공간의 개별 `ceilingHeight_mm`, 없으면 층 기본. 낮춤은 [50, 천장고−50] 클램프.
  매스라서 견적 물량(spCeilArea 등)에는 영향이 없다.
- **도구 버튼 (K)** `data-tool="ceil"` + 좌측 패널 `천정 (K)` 섹션 — 프리·빠른작업 토글(금색 강조),
  낮춤 입력 `#ceil-drop`(STATE.ceilDrop, 기본 300). 명령어 `cl|ceil|천정|천장`. STATE.ceilMode 기본 quick.
- **프리** = startRect 드래그 경로 재사용, endRect 의 ceil 분기가 `makeCeilingMass()`(tools.js).
- **빠른작업** = 방 안 한 번 클릭 → `ceilQuickAt()` 이 공간 폴리곤을 복사. 방 밖 클릭은 토스트 거부.
- **함께 고친 실사용 버그**: `setTool()` 이 select 밖으로 나갈 때 `renderSpaceHandles()` 호출 —
  이전엔 남은 꼭짓점 그립이 그리기 도구의 mousedown 을 삼키고(빠른작업 방 클릭 포함) 드래그도 받았다.
- 검증: 스위트 C01~C12, E2E `cdp-ceil.cjs`(실버튼·실포인터 — 프리 드래그/빠른작업 클릭/낮춤 입력/
  방밖 거부/미니폼 2판 elev), 노드 계약 `면 스냅 16px` 현행화. 데스크톱 1313·터치 1315·노드·
  파랑축·Z왕복·계단·단독 프리폼 전부 통과. v=tablet111.

### 계단 — 미니폼이 평면 도식과 같은 방향 (2026-09-08 대표 지적)
- **문제**: 3D 가 긴 변 방향으로 리저 180 규칙의 제멋대로 계단을 세워 평면 도식과 방향·단수가 달랐다.
- **고침**: `buildStairs` 가 2D `spaceStairInfo`/`buildSpaceStairShape` 규약을 그대로 따른다 —
  같은 틀(방 OBB, `stairFrameOf`=2D `_polyFrameMm` 포팅) · 같은 단수(트레드 280·stepCount) ·
  `rot` 90° 단위·`mirror` 동일 변환(`_xformShape` 순서: 미러→회전) · 방이 돌면 계단도(obj.rot=OBB 각).
- **유형별**: I=틀의 위(-y)가 높다 · L=좌상 참(W×W)·가로 플라이트 바깥이 최고 · U=위 참(전체 폭)·
  오른 아래가 최고. **upDir 는 화살표 표기일 뿐 높이 분포는 같다** — UP 은 화살촉이, DN 은 출발
  동그라미가 이 층 바닥과 만나는 높은 끝인데 같은 자리다. 600mm 미만 방엔 계단이 안 선다(2D 동일).
- 검증: node [S1~S8] 손계산(단수 15=4200/280·rot90·mirror 참 위치·U 최고/최저·30° 회전 방·upDir 불변) ·
  `cdp-stair2.cjs` E2E 8조합 — **페이지의 진짜 2D 도식 함수 출력(출발 동그라미)과 3D 낮은/높은 끝을
  절대좌표로 대조, 전부 30mm 안팎 일치**. 캐시 `3d37`.

### 내력벽 = 해칭 전용 (2026-09-08 대표 지시)
- **"내력벽은 평면 도면상 해칭 표현으로만 사용되고 어떤 것에도 영향을 미치지 않는다."**
  이미 빠져 있던 것: 견적 벽면적(`spWall`)·입면(`elevationWallsOf`)·절단 입면·자동 면적 감지·겹침 검사.
  이번에 추가로: **미니폼에 벽체가 안 만들어진다**(build3d 가 bearing 을 건너뜀 — 그 위 문·창도
  도면 표기일 뿐 3D 에 없음), 벽 수 집계 제외, '모든 벽에 마감 적용' 제외.
- **레이어 '내력벽 (해칭 전용)'** 추가 — 2D 레이어 패널에서 끄고 켠다. 해칭(병합 캔버스·개별·중심선)이
  전용 Konva 그룹(`groups.bearing`, 일반벽 위 z-order)으로 옮겨져 한 번에 숨는다. 기본 켜짐,
  옛 저장본은 기본값으로 보정.
- 폐기: 종전 '내력벽 무게중심 3D 정렬' 시험 — 벽체 자체가 없어졌으므로 부재(不在) 시험으로 교체.
- 검증: `?test=1` [B01~B08](레이어 존재·토글·해칭 그룹·벽면적 불변·패널 줄·3D 부재·벽 수) ·
  node 계약 4건. 데스크톱 1301/1301 · 터치 1303/1303. 캐시 `tablet110`/`3d36`.

**프리폼 로드맵 완주**: ①독립 문서 → ②면 위 그리기 → ③파내기(벽감·관통) → ④Follow Me(몰딩) →
⑤그룹·컴포넌트 → +재질 페인트 → +스테이징 → +파랑 축·면 스냅 → **+독립 앱(/freeform)**.
남은 확장(원하면): 살아 있는 컴포넌트(정의-인스턴스).
- 검증(Z축 3층): `?test=1` **[Z01~Z24]** · `tests/minicad-3d.cjs` **[T1~T8]**(손계산 물매 4/10·기울기 21.8°) ·
  `cdp-grip.cjs` 그립 E2E 21건 · **`cdp-zround.cjs` 두 창 왕복 22건**(진짜 포인터로 그립을 끌어 평면이
  바뀌고 → 3D 가 mesh 로 다시 서고 → 패널에서 고치면 3D 가 따라오고 → 천장고 +500 에 매달린 점이
  두 창에서 함께 오르고 → undo 가 한 줄기). 캐시 `tablet109`/`3d25`.

### 단독 프리폼 = 스케치업 100% (2026-09-08 대표 지시 "여기는 100% 스케치업과 동일하게 만들어라")
- **범위**: `/freeform`(`/minicad/3d/?ff=1`) 단독 프리폼만. 연동 미니폼(미니캐드 안)은 종전 셸·동작 그대로 (`linked-view.cjs` 회귀).
- **먼저 고친 것**: 단독은 채널이 없어 `if(!chan)` 가드가 선·사각형·원·오프셋·undo·배치를 전부 막고 있었다
  ("MiniCAD 창이 없어 벽을 못 만듭니다"). 가드를 `canEdit()`(=채널 또는 프리폼)로 통일.
- **셸 교체(부팅 때, `ffStandaloneShell`)**: `index.html` 의 `<template id="ff-menus|ff-tools|ff-keys">` 로 메뉴 8개·큰 도구 세트 35버튼·
  단축키표를 스케치업 그대로 갈아 끼운다. 미니캐드 개념(평면 저장·천장·이름표·공간/벽 전환·견적)은 단독 화면에서 사라진다.
  강사 문구는 `FF_HINT`(단독 전용). 우클릭 메뉴는 `ffCtxItems`(그룹·컴포넌트·분해·재질·몰딩).
- **새 도구** (전부 emitEdit→ffApply 한 길): 다각형(C 와 같은 op, `Ns`=변 수) · 회전 사각형(3점) · 프리핸드(끌기, 닫히면 면) ·
  3점 호 · 파이(부채꼴 면 — 호+반지름 2선이 고리를 닫는다) · 각도기(각도 안내선) · 축(`ST.axesO` — 원점 스냅·[x,y] 절대좌표·축 표시) ·
  3D 문자(`text3dPolys`: 캔버스 래스터→경계 변 연결→`_rdpRing`→구멍은 열쇠구멍 다리→`massfrompoly` 한 그룹) ·
  단면(`ST.sections`, 재질 `clippingPlanes`, 숫자=밀기, 보기▸단면/단면 자르기) · 팔로우 미 도구(단면=벽면 위의 면 → 경로=매스 둘레,
  단면 평면이 경로 변과 수직이어야, op `sweep`) · 줌 창 · 카메라 위치(바닥 클릭→걷기 눈높이) · 둘러보기 · 걷기 · 시야각 · 2점 투시 · 아래 뷰.
- **편집**: 매스 배율 S(`scale` op — 균등/Shift=높이만/`a,b,c`) · **G=그룹 · Shift+G=컴포넌트**(`mkcomp` — cid 로 인스턴스 추적) ·
  분해 · **컴포넌트 정의 갱신**(`compupdate` — 이 인스턴스로 다른 인스턴스 전부 재생성 = 라이브 링크) · Ctrl+페인트=면 하나(`facemat`,
  각기둥→다면체 승격, build3d 가 재질별 mesh prim 분리) · 매스 태그(`tag`, 태그 칸 ＋추가) · 줄자 재고 숫자=모델 전체 크기(`scaleall`) ·
  Ctrl+T 선택 없음 · Ctrl+N/O/S · K 모서리.
- **솔리드 도구** (`js/sketch.js` `massCSG` — BSP CSG, csg.js 알고리즘): 결합·빼기·교차·다듬기·분할. 스케치업 규약대로 빼기·다듬기는
  **첫째가 자르는 쪽, 둘째가 잘리는 쪽**(다듬기는 첫째 유지). 결과 조각은 같은 평면끼리 변을 이어 붙여(`csgMergeFaces`) 자유 매스로.
  오목 면은 `earTriangles`(원래 감김 유지 — 뒤집으면 CSG 가 깨진다, 실제로 그랬다) 로 build3d 가 삼각화.
- **표시**: 면 스타일 5종(와이어프레임·히든 라인·셰이딩·텍스처·단색, `applyFaceStyle` — 재질 원본은 `userData._fs0`) · 모서리(K) ·
  안개 · 숨은 형상(유령 와이어, 픽킹 제외) · 안내선 토글 · 단위(모델 정보 — mm/cm/m/in) · OBJ/STL 내보내기.
- **검증**: `tests/minicad-e2e/ff-sketchup.cjs` 53건(헤드리스 크롬, 진짜 포인터·키: 도구 전부·그룹/컴포넌트·단면·스타일·솔리드·새로고침 복원·콘솔 오류 0)
  · `linked-view.cjs`(연동 뷰 회귀) · `tests/minicad-csg.cjs`(부피 검증: 결합 6/빼기 2/교차 2/분할 2·2·2/관통 4.32/회전 매스) · 계약 · 2D ?test=1. 캐시 `3d35`→`3d36`.
- **정직한 한계**: 비균등 배율은 파냄(cuts)을 버린다 · 3D 문자 구멍은 열쇠구멍(폭 0.6mm 다리) · 히든 라인의 모서리는 25° 기준.
- **2차 (같은 날, 대표 재지시 "아직 부족하다")** — 매스의 면·꼭짓점을 직접 만진다 (`sketch.js` massFindFace/massPushFace/massExtrudeFaceNew/
  massVertXY/massRotate3/massFlip/massScaleAbout, `_massNormalize` 가 바닥 0·pts 동기·각기둥 복귀를 맡는다):
  · **밀기끌기 = 면 이동**(어느 면이든 법선으로, 매스가 늘고 준다 · 안으로 밀면 반대 면 10mm 앞에서 멈춤 · Ctrl=원래 면 두고 새 매스 · 더블클릭=반복)
  · 꼭짓점 그립 **Alt+끌기 = xy 이동**(각기둥이면 밑·윗면 짝 함께) · 원·다각형·호·3점 호·파이·회전 사각형·프리핸드를 **벽면·윗면 위에서도**(`shape3`, 평면 bag)
  · 회전 도구를 **세워진 면에 대면 그 면의 법선이 축**(`rotate3`) · 회전 중 Ctrl=복사 → x3=방사 배열 · **배율 그립**(모서리 8=균등·면 6=한 축·Ctrl=중심·숫자=배율 또는 `1500mm`=치수)
  · 뒤집기(빨강/초록/파랑) · 외곽 셸 · 매스 면·벽면 면 오프셋 · 클릭한 면 정보(개체 정보)
  · 미리보기는 전부 사본(lean)에 sketch.js 를 돌려 만든 고스트 — 보이는 것 = 확정되는 것.
  · **함정(실제로 겪음)**: (x,y↓,z↑)→세계(x,z,y) 는 반사라 `mesh` prim 의 삼각형 감김이 뒤집혀 `hit.face.normal` 이 안쪽을 가리켰다 →
    다면체 매스에서 단면·면 밀기·면 재질이 어긋남. primMesh 가 삼각형을 거꾸로 감아 해결. 헤드리스 E2E 는 아이소 뷰에서 옆면 점이
    윗면에 가려지므로 옆면 조작은 `setView('right')` 로.
- **3차 (같은 날)** — 재질: 스케치업 기본색 24 + 사용자 색(`C_RRGGBB`) + **이미지 재질**(`IMG_`, 512px JPEG dataURL 을 문서 `free.mats` 에 저장, 1장 폭 m 지정) ·
  Shift+페인트=같은 재질 전부 · Ctrl=면 하나(색상도) · Alt=추출(색 포함). 지우개가 안내선·단면·주석도 지운다. 단면 트레이(표시·✂활성·⇄뒤집기·✕).
  보기▸선택만 보기(isolate) · 장면 애니메이션 재생/정지(Esc) · 그림자 날짜(월)·밝기·어둡기 · 모델 정보 통계+정리(미사용 컴포넌트·재질) ·
  파일▸가져오기 OBJ(`ffParseOBJ` → op `massfromfaces`, y-up→(x,-z,y)) · 개체 정보 부피(다면체 실부피)·그림자 드리움/받음.
  검증 `ff-sketchup-2.cjs` 31건 · `ff-sketchup-3.cjs` 24건. 캐시 `3d37`.
- **4차 (2026-09-09, 대표 지시 "점·선·면·객체가 모두 하나하나 다 선택될 수 있도록")** — 스케치업 원시 기하 선택 그대로:
  · **클릭 = 커서 아래 가장 작은 요소**: 꼭짓점(8px) > 모서리(8px) > 면. 레이가 실루엣을 빗나가도 `ffPartNearScreen` 이 화면에서 가까운 꼭짓점·모서리를 잡는다(스케치업 픽 조리개).
    **더블클릭 = 객체(매스) 전체**, **트리플 = 연결된 전체**(gid 그룹), Shift/Ctrl+클릭 = 추가/제거(한 매스의 요소 여럿). gid 그룹은 스케치업 그룹처럼 클릭=전체 · 더블클릭=안으로(나머지 흐림 0.3).
    `ST.parts=[{kind:'vert'|'edge'|'face', id, p | a,b | lp,ln,…}]` — 인덱스가 아니라 **로컬 좌표**로 기억해 변환·재조립·이동 뒤에도 같은 자리를 다시 잡는다(`ffReselectFace`, `ffShiftParts`).
  · 선택한 요소: **M 이동**(면 하나=법선, 그 외=바닥 방향 · ←→↑=세계 축 · 숫자=거리 · op `moveverts`) · P 밀기끌기 · F 오프셋 · **B 페인트 = 그 면만**(객체 전체는 더블클릭으로 잡고 칠하면 통째; 선택한 면들 위를 클릭하면 전부) ·
    **Del = 삭제**(면→열린 껍질 `m.open`, 모서리/꼭짓점→붙은 면 함께) · 우클릭 면 뒤집기·면 재질 지움 · 개체 정보 = 면적·법선·재질 / 길이·끝점 / 좌표.
  · 선택 도구는 옮기지 않는다(매스 끌기 = 선택 상자) — 이동은 M. **선택 없이 M 로 면·모서리·꼭짓점을 잡으면 그 요소가 옮겨진다**(스케치업).
  · z 그립: 요소가 잡혀 있으면 안 세운다. 그립을 클릭만 하면(끌지 않으면) 그 꼭짓점을 선택. 바닥·벽면 스케치 점·선·면은 종전대로 객체 클릭 선택.
  · sketch.js: `massFaceRing/massEdges/massMoveVerts/massDeleteFace/massDeleteEdge/massDeleteVertex/massReverseFace`. 검증 `ff-sketchup-4.cjs` 21건. 캐시 `3d39`.
- **5차 (2026-09-09, 대표 지시 "선택한 면 위에 선을 그으면 면이 분할되도록")** — 스케치업 Divide + Push/Pull 그대로:
  · 매스 면 위에서 L 로 그은 선의 **양 끝이 그 면의 모서리(꼭짓점)에 닿으면 면이 둘로 나뉜다**(`massSplitFace` — 끝점은 변에 새 꼭짓점으로 끼우고
    그 변을 공유하는 이웃 면에도 끼워 균열이 없게, op `splitface`). 안쪽에서 안쪽으로 그은 선은 종전대로 면 위 스케치 선. 이어 그으면 또 나뉜다(끝점은 분할선에도 스냅).
  · **나뉜 반쪽 밀기끌기** = `_massExtrudeFaceInPlace`: 같은 평면 이웃(분할선 너머)과 공유하는 꼭짓점은 옮기지 않고 **복제해 옆면을 세운다**,
    꺾인 이웃과만 공유하면 옮긴다(이웃이 늘어난다), 꺾인 이웃에는 복제점을 변에 끼운다 → 계단·돌출이 스케치업과 같은 위상으로 나온다(전 면 평평 검증).
  · 검증 `ff-sketchup-5.cjs` 10건(옆면 세로 분할 → 반쪽 300 뽑기 부피 4.3·면 8 · 안쪽 선=스케치 선 · 윗면 가로 분할 → 반쪽 400 = 계단 4.8 · Ctrl+Z 단계). 캐시 `3d40`.
- **6차 (2026-09-09, 대표 지시 "이름표 기본 OFF · 스냅 표시 점과 선이 너무 두껍다 → 글로우 · 안내선 · 스냅 잘 걸리게")**
  · 단독 부팅 시 이름표 OFF. **글로우**: 스냅 표시·시작점·스케치 점 = 화면 크기 고정 발광 스프라이트(`glowSprite`, sizeAttenuation=false, `_pxScale`),
    스케치 선(바닥·벽면) = 1px 코어 선 + 얇은 발광 헤일로 원기둥(r 14mm, 집을 수 있게 Additive). 선 미리보기 막대도 얇게+발광. 리사이즈 시 `_glowResize`.
  · **함정(실제로 터짐)**: 선택 하이라이트 `_hl` 이 재질 사본에 emissive 를 무조건 넣었는데 MeshBasic·Sprite 에는 emissive uniform 이 없어 렌더 전체가 죽었다
    ("reading 'value'") → emissive 있는 재질만, 아니면 색을 밝힌다. 발광 점 스프라이트는 레이가 잘 안 맞아 `hitAt` 이 화면 8px 안이면 먼저 잡는다.
  · **안내점**: 줄자 Ctrl+클릭 = 안내점(십자+발광, `addGuidePoint`, 스냅 'guide', 지우개·편집▸안내선 삭제·보기▸안내선 토글 포함).
  · **스냅 보강**: 자유 층 스냅 후보가 다면체의 bbox 대신 z≈0 인 **진짜 밑둘레 꼭짓점·모서리**(massSolid) · 픽셀 반경 끝점 16/중간 14/선 12(면 위 16) · 격자 = 모델 정보의 격자 설정(`ST.gridMM`) ·
    **버그 수정**: `_snapDataOf('freeform')` 병합에 guides/xpts/gpts 가 빠져 있어 단독에서 안내선·교차점 스냅이 아예 안 걸렸다 → 합친다. 줄자·안내점은 빈 바닥에서도 된다. 검증 `ff-sketchup-6.cjs` 9건. 캐시 `3d41`.
- **7차 (2026-09-09, 대표 지시 "버텍스와 선의 표현이 너무 투박하고 크다 — 자연스럽고 모던하게")**
  · 모든 점 표시는 **화면 크기 고정 발광점**(줌해도 안 커짐): 스케치 점 7px · 스냅 마커 12px · 시작점 10px · z/배율 그립 9/8px(스프라이트, `_gripAt` 화면 9px 근접) ·
    선택 꼭짓점 11px · 모서리 끝 7px · 안내점 8px+작은 십자. 발광 텍스처는 밝은 코어+짧은 꼬리.
  · 스케치 선 = **안 보이는 집기 원기둥(r14, `userData.pick`, material.visible=false)** + 얇은 발광 헤일로(r4) + 1px 코어. 집기 메시는 `_hl`·`applyFaceStyle` 이 건너뛴다.
  · 선택한 면 = 옅은 파랑 판(0.22) + 1px 밝은 테두리, 모서리 = 1px 밝은 파랑. 선 미리보기 막대 6×3mm.
  · 매스 모서리(K) 단독 기본 ON — 얇고 은은한 선(0.55, 히든 라인에선 1). 캐시 `3d42`.
- **8차 (2026-09-09, 대표 지시 "z 파랑 축이 안 잡힌다 — 선 뽑을 때 z 값도 · R 은 두 번째 클릭 뒤 바로 높이")**
  · **파랑 축 자동 추론**: 땅에서 선을 긋다 커서를 화면의 파랑 축 방향(`_blueDir`, 12° 안·18px 이상)으로 끌면 카메라를 보는 세로 종이로 올라타
    세로 선이 된다(`ffBlueHop(op,auto=true)`, 축 고정 v). 숫자 = 높이. 방향을 벗어나면 되돌아온다(`_lineBeginAt` 로 바닥 선 복원). 세로 선을 하나 그으면
    그 종이에 남고 축 고정이 풀려 이어 그리고 닫으면 세로 면 → P. ↑ 수동 홉도 같은 함수. `_screenDir` 는 카메라 행렬을 먼저 갱신(스테일 행렬로 엉뚱한 방향이 나왔다).
  · **R = 상자 한 흐름**: 사각형(·원·다각형·회전 사각형·면 위 도형)이 생기자마자 `ffAutoExtrude` 가 그 면의 밀기끌기 단계로 들어간다 —
    위로 끌어 3번째 클릭 또는 숫자 = 높이, Esc = 면만. 기준점은 2번째 클릭 자리(`ST.lastPtr`)라 첫 이동부터 높이가 보인다. 벽면 위 사각형은 법선 뽑기(extrude3).
  · 검증 `ff-sketchup-7.cjs`. 캐시 `3d43`.
- **9차 (2026-09-09, 대표 물음 "높이에 한계가 있는 것 같은데" → "진행해줘")** — 한계 제거:
  · 끌기 = **줌 비례**(`_dragAlong`: 축을 화면에 투영한 방향 성분 × `mmPerPx`) — 면→매스(extrude)·매스 윗면·벽면 뽑기(extrude3)·벽 두께·꼭짓점 z 그립 전부.
    종전 5mm/px·3mm/px 고정은 한 번 끌기 상한이 4~5m 였다. 축을 정면으로 봐서 화면에서 1/4 이하로 눌려 보이면 마우스 위아래로 잰다.
  · 숫자 입력은 원래 상한 없음(하한 10mm). 카메라 far 400m→2km(평행투영 ±2km), 안개는 꺼져 있어도 모델 크기(자유 층 매스 높이 포함 `ffSpan`)에 비례.
  · 검증 `ff-sketchup-8.cjs` 5건(같은 100px 끌기가 멀리서 더 큼 · 30m 상자 · 정면 벽면 뽑기 · z 그립). 캐시 `3d44`.

### 하늘·바닥 배경 (2026-09-07 대표 지시 — "스케치업처럼 배경을 나누고 나중에 배경을 입힐 수 있게")
- **왜**: 단색 배경은 아무리 돌려도 도는 느낌이 없다. 스케치업은 배경을 **지평선에서 하늘과 바닥으로**
  갈라 칠해 어디가 위인지·카메라가 얼마나 기울었는지를 배경만으로 알려 준다.
- **방식**: 큰 공을 두르지 않고 **화면을 통째로 덮는 판 한 장을 본 장면보다 먼저 그린다**(`drawFrame`
  → `renderer.clear()` → 배경 씬 → 본 씬, `autoClear=false`). 색은 *카메라에서 그 픽셀을 향한 세계 방향*
  의 높이 성분 `d.y` 로 정하므로 **`d.y=0` 인 곳이 곧 참 지평선** — 돌리든 걷든 세계 바닥면(y=0)과 맞는다.
- **겪은 함정 3가지**(전부 `tests/minicad-3d.cjs` 가 잡는다):
  1. 반지름 900 짜리 공으로 두르니 **카메라 far 가 400** 이라 통째로 잘려 배경이 새까맸다 → 화면 덮개.
  2. `new THREE.Color(hex)` 는 색관리가 **선형값**으로 바꾼다. ShaderMaterial 은 색공간 변환 조각이
     안 붙어 그 값이 그대로 화면에 나가 어둡게 죽었다 → `_c` 는 **sRGB 바이트를 그대로** 쓴다.
  3. `syncSky` 가 `camera.matrixWorld` 를 **그리기 전에** 읽어 한 프레임 늦었다(카메라를 옮긴 직후
     한 번만 그리면 옛 자세로 칠해져 화면이 전부 땅) → `camera.updateMatrixWorld()` 먼저.
- **평행투영**: 모든 픽셀의 방향이 같아 계산이 한 색으로 뭉갠다 → 평행투영일 때만 **55° 가상 화각**
  으로 방향을 만든다(스케치업 평행투영도 지평선을 눈높이에 그린다).
- **주·야**: `SKY_PRESET.day/night`, `setNight` → `applySkyColors`. 안개 색도 지평선 색을 따라간다.
- **배경 그림**: `setSkyImage(url)` 이 가로 2:1 파노라마를 방향으로 찍어 배경에 두른다
  (셰이더 `uHasTex`). 메뉴 **보기 › 배경 그림 불러오기…**, 파일 한 장 고르면 끝.
- **조작**: 스타일 패널 **🌄 하늘·바닥** 버튼(누를 때마다 하늘↔단색), 메뉴 `mi-sky`.
  장면 저장·복원에 `sky` 종류가 함께 실린다. PNG 저장(`screenshot`)에도 배경이 붙는다.
- 캐시 `3d25`.

### 점·선·면 → 매스 (2026-09-04 대표 지시 — "선·사각형·원이 바로 객체가 되면 안 된다", 프로토콜 6)
- **원칙(스케치업 동일)**: 선·사각형·원·호·다각형 도구는 **객체를 만들지 않는다**. ① 점(x,y)+선 → ② 선 고리가 닫히면 **면**(자동 검출) → ③ 면에 **Z** 를 주면 객체. 2D·미니폼 어느 쪽에서 그려도 같은 규칙(왕복 동기화).
- **엔진 `js/sketch.js`**(state.js 뒤·engine.js 앞 로드): `STATE.sketchPts[{id,x,y}]`·`sketchEdges[{id,a,b}]`(평면 그래프 — 교차·접합 자동 분할)·`sketchFaces[{id,pts}]`(`skDetectFaces` 최소 고리). API `skAddEdge/skAddRect/skAddCircle/skAddPoly/skRemove(kind,id)/skRemoveEdge/skRemoveFace/skClear/skFaceArea/skFacePoly/skEdgePts/skObb/skGuessKind`(폭≤450·길이/폭≥2.5 = 벽) / `skExtrude(faceId,z,as)` / `massFromPoly` / `massConvert(id,as)` / `massAbsPoly`. 스냅샷·자동저장·JSON·층 시트 모두 네 배열을 포함(옛 문서는 빈 배열로 보정).
- **면 + Z 의 기본 결과 = 자유 매스**(`STATE.masses` `{id,name:'매스N',x,y,angle,pts(상대),h_mm,elev_mm,color,locked}`, 2D kind `masses`, 3D kind `mass`, 회색 `#B9C6D2`). 스케치업 푸시/풀 솔리드와 같이 **이동·회전·복제(Ctrl+끌기)·P 로 더 밀기·띄움(elev_mm 적층)·잠금**이 자유롭고, 필요할 때 **공간(바닥·천장·둘레 벽, 천장고=Z)** 또는 **벽(높이=Z·두께=짧은 변)** 으로 전환(`massConvert` — 2D 속성 패널 [공간으로]/[벽으로], 미니폼 속성 패널·우클릭 "매스 → 공간/벽"). `as` 옵션: `solid`(기본)·`space`·`wall`·`auto`(skGuessKind).
- **2D 흐름**: 도구로 그리면 `skAfterFace` → 면 선택 + 명령창 `sk-z` 모드("Z 높이(mm):", Esc = 면만 유지) / 면 선택 시 속성 패널에 Z·무엇으로(매스/공간/벽/자동)·[⬆ 객체 생성]·[면 삭제]. 명령 `sk on|off|clear`, `ex 2700`(선택 면 올리기). `sk off` = 옛 즉시 객체 생성(`STATE.sketchMode=false`). 그리기 스냅은 스케치 점·선 중점도 후보. 선택 우선순위 점 > 선 > 매스 둘레 > 면 안쪽. 렌더 그룹 `groups.masses`(공간 위·벽 아래)·`groups.sketch`(벽 위).
- **미니폼 흐름**: L/R/C/호/옵셋 → `sketchline{x1,y1,x2,y2}` / `sketchrect{x1,y1,x2,y2}` / `sketchcircle{cx,cy,r,n}` / `sketchpoly{pts}` (낙관적 고스트 `spawnPendingSketchLine/spawnPendingFace`, 다음 build 때 실물 교체). 선 고리가 닫히면 면 고스트. **P(밀기끌기)를 스케치 면에** → 프리즘 고스트 끌기(VCB 숫자 입력 가능) → `extrude{id,z,as:'solid'}`(활성 층만; 잠든 층은 거부+안내). 매스에 P = `set h_mm`, 더블클릭 = 직전 Z 반복(`ST.lastPP`). 속성 패널: 면 = Z 입력+[객체 생성], 매스 = 이름·색·높이(Z)·띄움·회전·공간/벽 전환. 삭제 = `sketchdel{kind,id}`(스케치) / `delete kind:'masses'`.
- **build3d.js**: `normalizeDoc` 가 sketchPts/sketchEdges(좌표 풀어 둠)/sketchFaces(`{id,polygon}`)/masses 를 정규화 → `buildSketchFace`(반투명 판 z=2·투명도 0.30) / `buildSketchEdge`(막대 18×14) / `buildSketchPt`(원기둥 r32) / `buildMass`(`{t:'prism',pts,h,z}` → ExtrudeGeometry, elev·angle 반영, 이름표 '매스N H h'). 층 높이·범위 계산에 매스 포함(적층 시 뚫림 방지). counts `sketch`·`masses`.
- **프로토콜 6 op**(`MC_PROTO=6`/`MF_PROTO=6`): `sketchline sketchrect sketchcircle sketchpoly extrude sketchdel sketchclear massconvert` — ui.js `_apply3DSketch`. 매스 편집은 기존 generic op(`move/set(h_mm,elev_mm,name,color)/delete/clone/lock/rotate`)에 `kind:'masses'`.
- **검증**: `tests/minicad-3d.cjs`(sketch.js 엔진 + 프로토콜 짝 + 조립 좌표/회전/라벨), `?test=1` **[SK] 39건**(면 검출·분할·삭제·extrude solid/space/wall/auto·massConvert·undo·자동저장 왕복·apply3DEdit 전 op·잠든 층), `cdp-3d.cjs` SKETCH 블록(R/L/C 가 sketch* 만 보냄·P=extrude·매스 set/massconvert·sketchdel·addspace/addwall/addcircle 금지), `cdp-face.cjs` 점·선·면 → 매스 E2E(3D 사각형 → 2D 면 25ms → Z → 매스 → undo 왕복). 캐시 `tablet104` / `3d18`.

### 3D v2 — 직접 편집·한 창 분할·증분/유휴 렌더 (2026-09-03)
- **한 창 통합**: [🧊 3D] 클릭 = 우측 분할 패널(`#pane3d` iframe → 3d/index.html, `body.split3d` + `handleResize()`), Shift+클릭/`3d tab` = 별도 탭, `3d off` 닫기. iframe 은 첫 열기에만 src 지정(지연 로드).
- **3D 직접 편집**: 조감에서 가구·기구·조명·전기·설비·기둥을 **드래그=이동**(10mm 스냅, 층 바닥 평면 레이캐스트), R/속성패널=15° 회전, Del=삭제, 속성 패널(#props)에서 벽 높이·마감 / 공간 바닥재·천장재·천장고 / 문창 폭·높이·창턱 / 다운라이트 인치 수정 → BroadcastChannel `{type:'edit',op,kind,id,floorId,patch}` → **ui.js `apply3DEdit`** 가 평면에 반영(활성 층=STATE+saveHistory→undo 가능, 잠든 층=floors[].data, 잠금 보호, set 은 화이트리스트, 벽·공간 delete 거부). 반영 → push3D → 3D 재조립으로 루프 완결.
- **층별 증분 재조립**: 뷰어가 `MC3D.splitFloors`+`buildFloorScene` 로 층마다 그룹을 만들고 층 JSON 해시가 같으면 재사용(`ST.floorCache`) — 한 층 수정 시 다른 층 메시는 그대로.
- **유휴 렌더 0**: needRender 플래그 — 카메라·드래그·장면 변화 때만 render, `shadowMap.autoUpdate=false`(변화 시 needsUpdate). 대형 도면 자동 성능(메시 3500↑ 그림자 자동 OFF·pixelRatio 하향, 사용자가 그림자 버튼 만지면 자동 해제).
- **조작**: 더블클릭=그 객체/방으로 줌, 시점 프리셋 ⌂⬓⬒◨(키 3/4/5/6), 🌗 주/야 무드, 걷기 유지.
- 주의: 뷰어 구조가 root>층그룹>객체그룹 2단 — 스모크·테스트는 `MC3DVIEW.objCount()`/`ST.floorCache` 를 본다(`ST.root.children`=층 수). 3d/index.html 은 이제 data.js 도 로드(재질 드롭다운).

### 스케치업식 도구 체계 (2026-09-03, 대표 결정: "스케치업과 동일 아니 그 이상")
- 좌측 도구 바(#tools) + 단축키: **Space 선택 · M 이동 · Q 회전 · P 밀기끌기 · B 페인트 · E 지우개 · T 줄자**. 마우스: 휠버튼 드래그=궤도, 우클릭 드래그=팬, 휠=줌(+빈 곳 좌드래그 궤도 유지).
- 방식도 스케치업식: **클릭-이동-클릭(스티키)**, 동작 중 **숫자 입력(VCB #vcb)=정확한 값**(이동 mm·회전 °·밀기끌기 mm, Enter 확정), 이동 중 **←→=축 고정**, Esc=취소, Shift+Z=전체 보기. 다음 클릭=확정(회전·밀기끌기·스티키 이동은 pointerdown 에서 commit).
- 밀기끌기: 벽 클릭→위아래 = `height_mm`(미리보기는 그룹 scale.y), 천장 클릭 = 그 공간 `ceilingHeight_mm`. 25mm 스냅, 최소 300.
- 페인트: 팔레트(#paintpal, 벽/바닥/천장 재질 — data.js)에서 고르고 면 클릭 → set finishMaterial/floorMaterial/ceilingMaterial (견적 연동).
- 줄자: 두 점 클릭 → mm/m 표시 (THREE.Line, depthTest off).
- **그 이상**: 모든 편집이 평면·견적 실시간 반영 + **Ctrl+Z/Y 가 MiniCAD 히스토리로 왕복**(apply3DEdit op:'undo'/'redo').
- **함정(실제로 당함): 클래식 스크립트(data.js)의 최상위 const 는 `window.X` 로 안 잡힌다** — 모듈에서는 전역 렉시컬 식별자로 직접 참조(`typeof X!=='undefined'?X:null`, view3d.js MATS). window.WALL_MATERIALS 는 undefined 라 팔레트가 비어 있었다.
- 도구 키(M/Q/P/B/E/T/Space)는 조감 모드에서만 — 걷기 모드의 Q/E(위아래)와 충돌 방지.
- 2026-09-03 추가(대표 지시 "스케치업 구조 정확히 카피 + 3D 는 별도 창"):
  - **[🧊 3D] 클릭 = 별도 창이 기본**(open3DView), Shift+클릭/`3d split`=분할 패널, `3d`=별도 창.
  - 스케치업 정합 추가: **S=배율**(가구·기구·설비 footprint w/h — apply3DEdit set 화이트리스트에 w/h, SCALABLE set), **O=궤도(선택) · H=팬 · Z=줌**(orbit.mouseButtons.LEFT 전환), **Ctrl+이동=복사**(op:'clone' — 사본 미리보기는 g.clone(true), MiniCAD 가 JSON 복제+makeId; movable 종류만), **밀기끌기 더블클릭=직전 값 반복**(ST.lastPP), 하단 #hint = 도구별 수정자 안내(스케치업 상태바 식).
  - 미구현(의미 모델과 충돌): Line/Rectangle/Circle/Arc 그리기(면·엣지 모델링), Offset, 그룹/컴포넌트 — 작도는 2D 평면이 담당.
- 2026-09-03 대지 제거 + 선/사각형 도구 (대표 지시):
  - **대지(전체 바닥 슬래브) 생성 안 함** — build3d 의 `_slab` 객체 제거, 방 바닥 폴리곤만. Blender 렌더에서도 튀어나온 받침판 사라짐. 그림자 받는 면은 방 바닥.
  - **L=선(벽 그리기)**: 3D 에서 클릭-클릭으로 벽 사슬 그리기(스케치업 Line) — 5° 안 직교 자동 스냅·Shift 강제 직교·숫자=길이(mm)·Esc/더블클릭=끝. **R=사각형**: 두 모서리 클릭=벽 4면(히스토리 꼬리 병합으로 Ctrl+Z 한 번에 취소 — 50개 상한에서도 동작하도록 `splice(len-4,3)`).
  - 활성 층은 2D 와 같은 `addWall` 경로(공간 귀속·vertex 공유·교차 분할), 잠든 층은 f.data 에 vertex+wall 직접 생성. op:'addwall'/'addrect' → `_apply3DWall`(ui.js).
  - 키 재배치: L=선(조명 토글은 💡 버튼만), R=사각형(회전 15° 퀵키 제거 — Q 도구·패널 버튼 사용, add 모드의 R=고스트 회전은 유지).
- 2026-09-03 배치(➕/G) + GLB 의미 탑재:
  - **3D 에서 새 객체 배치**: ➕ 도구 → #addpal(가구/위생/조명/전기/설비 전 라이브러리) → 고스트(가구는 buildFurniture 실형상, 반투명) → 바닥 클릭=배치(연속), R=회전, Esc=끝. 층은 커서 아래 객체의 층 자동. → `apply3DEdit op:'add'` → **ui.js `_apply3DAdd`**: 라이브러리 검증·makeId·findNearestSpace/makeLayerName·다운라이트 인치·선형등 length_mm, 잠든 층은 f.data 에.
  - **GLB**: 재질 이름 `MC_<hex>[_glass|_emit]`, 객체 그룹 extras.ecorean={kind,type,material(마감 코드),size_mm,wall_mm,floor} — Blender 파이썬이 코드→텍스처 자동 매핑 가능. 내보내기는 root.clone 후 sprite/광원 제거·userData 치환(원본 장면 무손상).
  - **함정 재발**: `LIBS` 도 window.FURNITURE_LIB 로 집다가 전부 null — **뷰어의 가구가 그동안 400×400 기본 상자로 세워지고 있었음**(이름·규격·팔레트 전멸). data.js/library.js 등 클래식 스크립트 const 는 반드시 typeof 전역 식별자로.
- 2026-09-03 텍스처·전층 견적·Blender 렌더:
  - **바닥 프로시저럴 텍스처**: build3d 바닥 프림에 `mcode`(floorMaterial) → 뷰어 `floorMat(code)` 가 캔버스 텍스처 생성(원목 널결/타일 줄눈 300·600각/마블 결/카펫 노이즈, 코드별 캐시). ShapeGeometry UV=m 단위라 `tex.repeat=1/S`(S=한 장의 실크기 m). 벽은 평색 유지.
  - **전층 합산 견적**: `_withFloorData(f,fn)`(ui.js — 잠든 층 데이터로 STATE 배열만 잠시 스왑, finally 복원) + `buildAutoEstimateAll()`(estimate.js — priceKey 별 물량 합산·층별 내역). 견적 카드에 [현재 층|Σ 전층 합산] 토글, `sendToEstimateOS` 는 다층이면 `estimateAllFloors` 동봉. 테스트 [FE] 5건(스위처 누수 검사 포함).
  - **Blender 2단계 (검증됨)**: `scripts/blender-glb-render.py` v2 — `blender -b -P ... -- in.glb out.png [samples] [view] [light]`
    - view: `iso`(낮은 앙각 외관)·`front`·`top`·`room`(최대 방)·`room:거실`(이름 부분 일치 — **실내 눈높이 1.5m 광각 컷**)
    - light: `day` / `night`(월드 어둡게 + 발광 40 — 조명 기구가 주인공)
    - 재질: GLB 에 실려 온 바닥 캔버스 텍스처는 유지(광만 조정), 없을 때만 Wave/Brick 프로시저럴. MC_*_glass/_emit → 유리/발광
    - extras 두 형태 수용: 정식 [⬇GLB]=`extras.ecorean`, 뷰어 루트 직접 parse=`extras.obj`(meta 포함) — `eco_of` 가 정규화
    - **Blender 5.2 실증**: 외관 iso + 거실 실내 주간/야간 렌더 성공(다운라이트 점등·코브 글로우·창 주야 표현·널결 바닥, 방 8개 자동 인식, 64샘플 ~15초)
    - 명령 예: `"C:/Program Files/Blender Foundation/Blender 5.2/blender.exe" -b -P scripts/blender-glb-render.py -- 도면.glb 거실.png 128 room:거실 night`
- 2026-09-04 **스케치업 동등화 완료 (인터페이스·도구·이동 컨트롤 전부, 프로토콜 5)** — 대표 지시 "인터페이스 및 모든 도구·이동 컨트롤을 스케치업과 동일하게, 미니캐드 호환 병행":
  - **선택**: Shift/Ctrl+클릭 추가·제거(`select(g,{toggle})`), **선택 상자**(`boxSelect` — →끌기=완전 포함, ←끌기=걸침 점선 `#selbox.cross`), 더블클릭=방(면+벽) `selectSpaceGroup` / 벽 이웃 `selectWallNeighbors`, 트리플=층 전체, Ctrl+A `selectAll`. 다중 선택 Entity Info 요약(종류별 개수·일괄 회전/잠금/복사/삭제/숨기기), 이동·회전(대표 중심 공전 — ui.js rotate 가 x,y 동반)·페인트·삭제 전부 다중 적용, `sendBatch` → ui.js `_apply3DBatch`(Ctrl+Z 한 번, `histSeq` 로 50 상한과 무관).
  - **우클릭 메뉴** `showCtx`(#ctxmenu — 개체정보·지우기·숨기기·잠금·회전·방/이웃 선택·전체보기·모두 보이기), Del/Backspace, **Shift+H 숨기기 · 편집▸모두 보이기**(`ST.hidden`), **잠금** op `lock`.
  - **Ctrl+C/X/V**: `copySel` → `ST.clip`(종류·규격·상대 위치), `pasteClip(ST.lastPtr)` = 마우스 위치에 batch add(인치·elev·w/h·flipped 동반 — `_apply3DAdd` 확장). **Ctrl+S = 평면 저장**(`{type:'save'}` → ui.js `_autosaveNow`), Ctrl+Shift+S=PNG.
  - **그리기**: L 선 사슬(`ST.op.chain`, 시작점 복귀=면 `addspace {pts,absorb,merge}` — 사슬 벽 흡수·Ctrl+Z 한 번), **C 원**(`addcircle {cx,cy,r}` → 2D `addCircleSpace`, `6s`=다각형), **A 호**(3클릭 → 벽 조각 batch, `arcPts`), **F 오프셋**(`offsetPoly` 안쪽 면), **D 치수 · 문자**(3D 주석 `ST.annots`). 추론: 끝점>원점>교차점>중간점>선/안내선, 픽셀 기준 반경(`mmPerPx`), **Shift=방향 고정**, ←→ 축, **↓ = 가까운 벽에 평행/수직**(`_guideDirAt`), from-점 추론(점선), 50mm 내 닫힘 힌트. VCB: `[x,y]` 절대·`<dx,dy>` 상대·`3000,2000` 사각형·음수=반대.
  - **줄자 T = 안내선**: 선/중간점에서 시작 → 평행 안내선(숫자=간격), Shift=두 점 안내선, `ST.guides`(점선 0x9A9AFF)·스냅 'guide'·편집▸안내선 삭제.
  - **VCB 후속 입력**(`vcbPostOn/Enter`): 동작 확정 직후 숫자=되돌려 그 값으로, **x3 · *3 · /3 = 배열 복사**(`arrayCopy` — 외부/내부 배열, `ST.lastMove`).
  - **문·창 = 벽 위 슬라이드**(`beginSlide`, build3d meta.wall/along → `set {x,y}`), **밀기끌기 벽 옆면 = 두께**(`set {thickness}` 30..600), 회전 도구 2단계(중심 클릭→각도기 `_protractor`), 배율 Shift=비균등.
  - **카메라**: 궤도 도구 O(좌드래그), 배면/좌/우(7/8/9), **이전/다음 시점**(`camPush/camPrev/camNext`), **평행 투영**(`setOrtho` — OrthographicCamera 교체·`_orthoFit`), Shift+Z 전체.
  - **표시**: **X-ray**(X — `setXray`, 재질 공유 대비 `material.userData._xr` 에 원본 저장), 태양 시각 슬라이더 `#st-sun`(`setSunT` → 그림자 방향·길이), **태그(레이어)** `#tags`(`TAG_OF`·`ST.tags` → `refreshVisibility`), **아웃라이너** `#outliner`(층>객체, 클릭 선택·더블클릭 확대, 열려 있을 때만 렌더 `_olDirty`), **장면** `#scenes`(localStorage `minicad.3d.scenes` — 시점·모드·투영·야간·천장·X-ray·태양·층).
  - **단축키 표(?)·메뉴·툴바(19 버튼)·트레이 8 섹션** 갱신. `MC3DVIEW` 는 `get camera()`(카메라 교체 대응) + select/boxSelect/sendBatch/setOrtho/setXray/copySel/pasteClip/addGuide/sceneAdd/… 노출.
  - 검증: `tests/minicad-3d.cjs`(프로토콜 짝·op 목록·index.html DOM·함수 존재), `?test=1` [ED] +25건(batch/rotate xy/lock/add 규격/thickness/opening xy/addspace pts·absorb·merge/addcircle/save), `cdp-3d.cjs` SKETCHUP 블록(전체선택·상자·우클릭·X-ray·투영·장면·아웃라이너·복사/붙여넣기·단축키). 캐시 `tablet103` / `3d17`.
`engine.js` 가 전역(stage, container, groups…)을 만들고 tools/ui/touch 가 이름으로 참조 — **include 순서 변경 금지**.
초기화: `index.html initApp()` → `initKonva()` → `initTools()` → `initUI()` → `initTouch()`.

## 작업 절차
1. `sites/net/public/minicad/` 에서 수정 (다른 곳에 사본 만들지 않는다)
2. `index.html` 의 `?v=tabletN` 캐시버스터 일괄 bump (`sed -i 's/?v=tablet3/?v=tablet4/g' index.html` 식)
3. 로컬 검증: 정적 서버로 열어 `?test=1` 통과 확인 (데스크톱·터치 프로필 모두). 인증 게이트(`/ecorean-gate.js`, `/app-cloud.js`)는 스텁으로 대체 가능
4. 레포 `npm test` → commit → push(master)
5. 배포: Vercel 은 Git 미연결 → 레포 루트(또는 클린 worktree)에서 `vercel --prod --yes --scope ecorean` → `curl https://ecorean.net/minicad/ -H "Cookie: sb-gdcfqbdgubgpzusbtftf-auth-token=x"` 로 캐시태그 확인

## 헌법 (MiniCAD 적용분)
mm 정수만 · 단가 추정 금지(NEEDS_RESEARCH) · 방수 = CONDITIONAL · NEEDS_CONFIRMATION 누락 금지 · TDD(테스트 미통과 커밋 금지) · 출력은 평면 JSON 기본(2.5D 는 영업 토글, 인쇄/JSON/AI번들 시 강제 OFF) · promptKeyword 에 브랜드명 금지

## 이력 문서
- `HANDOVER_v5_8.md` — v5.6→v5.8 변경 요약 (구 분리본 시절 작성)
- `CLAUDE_CODE_KICKOFF_v5_8.md` — 초기 킥오프 (책임 범위·헌법 발췌)
- 이후 변경은 git log `-- sites/net/public/minicad` 가 기준
